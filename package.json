{
  "name": "clickup-docuseal-bridge",
  "version": "1.0.0",
  "description": "Bridge app that listens for ClickUp Agreement Status changes and sends documents via DocuSeal",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "setup-webhook": "node setup-webhook.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "marked": "^14.1.0"
  }
}
